# PRH Morphology Validation — FlyWire FAFB
# Core scientific stack
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# TDA
gudhi>=3.8
ripser>=0.6
persim>=0.3
umap-learn>=0.5

# Neuroscience / skeleton analysis
navis>=1.8          # NBLAST + SWC loading (Costa et al. 2016)
networkx>=3.1

# morphoclass (install separately — see upstream README)
# pip install -e .  (from the morphoclass repo root)
