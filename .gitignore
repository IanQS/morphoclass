# Byte-compiled / optimized / DLL files
*$py.class
*.py[cod]
__pycache__/
cov.xml
report.xml

# Other python non-code
.ipynb_checkpoints
.ipython

# Home folder files
.bash_history
.cache
.keras
.local
.vscode

# Egg files
*.egg-info
.eggs

# pytest
.coverage*
.pytest_cache*
htmlcov

# Local files and folders
*.log
.DS_Store
.env*
!.env.example
/.idea/
/.tox/
/build/
/dist/
/docs/build/
/logs/
/src/morphoclass/version.py
/venv/

# pyenv file
.python-version

# PRH experiment — generated outputs (never commit)
outputs/

# Source archives (extracted into repo; originals not needed)
*.zip

# Stray root-level copy of experiment script
/00_sanity_checks.py

# PRH experiment — full FlyWire dataset (13 GB, download separately)
data/fafb/

# PRH experiment — model binaries and large numpy arrays
experiments/fafb_perslay/**/*.npz
experiments/fafb_perslay/**/*.npy
experiments/fafb_perslay/**/*.pt

# SLURM logs (generated at runtime)
outputs/fafb/logs/

# Sample metadata IS committed; SWC binaries are not (regenerate via sample_dataset.py)
!data/fafb_sample/
data/fafb_sample/swc/

# Progress report zips (ephemeral)
progress_report/
progress_report_*.zip
outputs/fafb_n*_backup/
